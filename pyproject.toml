[project]
name = "interval-calculator"
version = "1.0.0"
description = "Interval Analyzer & Plotter"
requires-python = ">=3.9"
dependencies = [
    "flet==0.24.1",
    "sympy",
    "matplotlib",
]

[tool.flet]
product = "Interval Calculator"
org = "com.proo0"
bundle_id = "com.proo0.intervalcalculator"
build_number = 2

[tool.flet.app]
path = "src"

[tool.flet.android]
extract_packages = ["matplotlib"]
