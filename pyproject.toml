[project]
name = "interval-calculator"
version = "1.0.0"
description = "Interval Analyzer & Plotter"
requires-python = ">=3.9"
dependencies = [
    "flet==0.24.1",
    "watchdog>=4.0.0,<5.0.0",
    "sympy",
    "matplotlib",
]
